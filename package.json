{
  "name": "proximo-lance",
  "version": "1.0.0",
  "private": true,
  "description": "Torneios de xadrez valendo rating FIDE perto da sua cidade no Brasil (dados da CBX)",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "install:all": "npm install && npm --prefix server install && npm --prefix web install",
    "dev": "concurrently -n api,web -c green,yellow \"npm --prefix server run dev\" \"npm --prefix web run dev\"",
    "build": "npm --prefix web run build",
    "start": "npm --prefix server start",
    "build:municipios": "node server/build-municipios.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
